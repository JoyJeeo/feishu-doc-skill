# 仓库状态

> 最近更新：2026-09-13
> 状态口径：截至本文件最近一次维护时，经仓库文件、Git 状态和测试结果核对后的项目快照。

## 当前阶段

- M5 跨资源项目工作区已完成，五个 Spec 均通过验证和用户人工验收；M6 尚未开始。
- M0 产品基线：已完成。
- M1 安全执行闭环：已完成。
- 最近完成父项：产品需求 `M5`，五个有序 Spec 均已完成。
- `SPEC-M2-01` 文档读取模型与质量审计：已完成并通过用户人工验收。
- `SPEC-M2-02` 内容生成、模板与创建预览：已完成并通过用户人工验收。
- `SPEC-M2-03` 章节编辑、排版与文档内表格：已完成并通过用户人工验收。
- `SPEC-M2-04` 真实文档集成验收：已完成并通过用户最终人工验收。
- `SPEC-M3-01` 表格 MCP 能力基线与实施契约：已完成。
- `SPEC-M3-02` 多维表格读取与预览模型：已完成并通过用户人工验收。
- `SPEC-M3-03` 多维表格真实集成验收：已完成并通过用户人工验收。
- `SPEC-M3-04` 电子表格限定范围查找替换：受阻，等待现有 Sheet 样例数据完成真实验收。
- 原 `SPEC-M3-05`：未开始且已移出当前路线图，保留记录但不作为 M3 退出条件。
- `SPEC-M4-01` 知识管理 MCP 能力基线与实施契约：已完成并获用户确认。
- `SPEC-M4-02` Wiki 与 Drive 只读模型：已完成并通过用户验收。
- `SPEC-M4-03` Wiki 与 Drive 写入预览：已完成并通过用户验收。
- 原 `SPEC-M4-04` 妙记读取与 M4 验收：未开始且已移出当前路线图，保留记录但不作为 M4 退出条件。
- `SPEC-M5-01` 跨资源能力基线与实施契约：已完成。
- `SPEC-M5-02`：已完成并通过用户人工验收。
- `SPEC-M5-03`：已完成并通过用户人工验收。
- `SPEC-M5-04`：已完成并通过用户人工验收。
- `SPEC-M5-05`：已完成并通过用户最终人工验收。
- M6：未开始。

## 已完成

- 产品资料：`.ai/README.md`、`.ai/DECISIONS.md`、`.ai/PRODUCT.md`、`.ai/ROADMAP.md`。
- Skill 入口：`SKILL.md`。
- 公共规则：MCP 能力、身份、预览、安全和质量规则。
- 工作流：`analyze`、`preview`、`apply`、`verify`。
- 本地安全辅助脚本：`scripts/feishu_guard.py`。
- 本地安全行为测试：`tests/test_feishu_guard.py`。
- AI 接续规范：根目录 `AGENTS.md` 要求新任务读取产品资料、核对仓库状态并持续维护本文件。
- AI 开发规范：产品需求与本地 Issue 均可作为开发入口；同一时间只实施一个父项，大任务按 Spec 拆分并逐段确认授权。
- AI 人工验收规范：每项新 Skill 能力完成实现后，必须在指定测试知识库展示真实效果并由用户明确验收，才能标记为“已完成”。
- AI 执行环境规范：所有仓库相关 shell 命令必须在 Conda `leju` 环境中执行，并优先使用可审计的 `conda run -n leju <command>` 形式。
- M1-B 能力发现：已核对当前云文档 MCP 前缀、用户身份参数和最小文档读写验证链路。
- M1-B Spec 映射：`SPEC-M1B-01` 至 `SPEC-M1B-03` 已记录在路线图和独立 Spec 文件中，三个 Spec 均已完成。
- M1-B 真实闭环：已完成 Wiki 解析、用户身份读取、预览确认、冲突检查、局部文本写入和回读验证。
- M2 Spec 映射：`SPEC-M2-01` 至 `SPEC-M2-04` 已记录在路线图和独立 Spec 文件中。
- M2 文档读取模型：已定义完整分页、嵌套层级、源顺序、块 ID、格式元数据和未知块保护规则。
- M2 章节定位：优先使用块 ID，否则使用完整标题路径；无法唯一定位时停止。
- M2 质量审计：已实现只读工作流，覆盖结构、清晰度、一致性、完整性和保真限制，并要求位置与证据。
- M2 内容生成：已实现事实、推论、建议和待确认项分离，以及扩写、压缩和块级输出规则。
- M2 模板：已实现 PRD、会议纪要、项目主页、周报、技术方案和复盘六类结构。
- M2 创建预览：已接入完整内容、目标位置、待确认项、格式风险和回读计划，并增加确定性结构校验。
- M2 章节编辑：已新增 `references/docx/editing-formatting.md` 与 `workflows/docx-edit-preview.md`，并接入章节唯一定位、边界和目标块校验、章节外保护、未知块保护、格式降级披露、轻量表格结构校验及重复预览执行保护。
- M3 Spec 映射：当前执行映射为 `SPEC-M3-01` 至 `SPEC-M3-04`；原 `SPEC-M3-05` 已按范围决策移出。
- M3 能力基线：当前可见 27 个 Sheets 工具和 46 个 Bitable 工具；Sheets 缺少任意区域值读取与写入工具，Bitable 核心元数据、字段和记录读写工具可见。
- M3 运行时约束：已在 `references/common/mcp-capabilities.md` 记录 Sheets 能力缺口、禁止替代通道，以及 Bitable 业务主键、分页、字段校验、幂等和回读要求。
- M3 Bitable 本地模型：已新增完整分页、表和字段唯一定位、业务主键零/唯一匹配、字段类型与选项保护、数据质量检查和批量部分失败规则。
- M3 Bitable 预览门禁：已接入记录创建与更新的前态指纹、逐字段差异、只读字段拒绝、选项新增拒绝和写前重复校验。
- M3 Bitable 批量结果分类：已按计划顺序稳定区分成功、失败和未执行业务主键，并拒绝计划外或互相冲突的结果。
- M3 Sheets 本地模型：已实现电子表格与工作表唯一定位、矩形 A1 范围、纯文本整格查找、匹配清单和范围边界校验。
- M3 Sheets 预览门禁：已锁定精确替换参数、写前状态指纹、写后新旧值匹配集合和范围外人工检查；不宣称任意区域值读写。
- M3 Sheets 工作流：已新增限定范围查找替换参考与专用预览工作流，并接入 Skill、分析和预览路由。
- M3 范围收敛：用户明确要求 Skill 只实现当前 MCP 能力；不开发、更新或扩展 MCP，原 `SPEC-M3-05` 已移出当前执行映射。
- M4 Spec 映射：当前执行映射为 `SPEC-M4-01` 至 `SPEC-M4-03`；原 `SPEC-M4-04` 已按范围决策移出。
- M4 能力基线：当前可见 Wiki 16 个、Drive 52 个、Minutes 3 个工具；已记录 Wiki 节点、Drive 文件与版本、权限读取边界，Minutes 仅保留能力快照且不进入实现。
- M4 Wiki/Drive 只读模型：已接入 Wiki 节点解析与完整子节点分页、指定 Drive 文件夹清单与精确元数据、协作者和公开设置审计，以及可校验的默认位置配置。
- M4 Wiki/Drive 写入模型：目标范围已定义（Wiki 节点创建/复制/移动/改名、Drive 文件夹创建/复制/移动、文件版本创建），冲突检测与回读验证要求已纳入 `SPEC-M4-03`。
- M4 范围收敛：用户于 2026-09-13 明确决定暂不增加妙记功能；未验收的读取入口、本地工具选择和测试已撤下，Wiki/Drive 当前范围已完成并通过人工验收。
- M5 Spec 映射：已拆分为 `SPEC-M5-01` 至 `SPEC-M5-05`，依次覆盖能力基线、项目材料与只读审计、主页/文档集/周报、结构化台账和真实集成验收。
- M5 能力基线：Wiki 组织项目目录，Docx 承载主页、文档集和周报，Bitable 承载行动项、风险、决策和指标，Drive 提供精确定位与归档依据；每个资源继续独立预览、确认和回读。
- M5 能力边界：Sheets 不支持任意区域值读写，不能承载 M5 指标台账；Bitable 新建应用、数据表和字段仅有工具元数据证据，需后续 Spec 独立前向验收；妙记继续不纳入路线图。
- M5 项目审计模型：已新增 `references/project/audit-model.md`、`workflows/project-audit.md` 和 Skill 路由，使用本地证据映射区分事实、发现、建议和待确认项。
- M5 来源门禁：`validate-project-audit` 校验来源链接与资源类型、读取完整性、部分来源限制和每条非待确认结论的来源引用。
- M5 项目文档模型：项目主页作为 Wiki 索引和 Docx 子文档父节点；文档集按需创建，周报锁定报告周期和来源，子文档链接在资源回读后通过独立主页编辑预览补充。
- M5 项目预览门禁：`validate-project-preview-plan` 校验独立预览编号、前序依赖、新建资源链接依赖和周报周期。
- M5 项目文档人工验收：用户于 2026-09-13 检查项目主页、需求说明和周报后明确验收 `SPEC-M5-03`，并授权 `SPEC-M5-04`。
- M5 项目台账模型：已新增行动项、风险、决策和指标四类 Bitable 字段契约及独立建表预览工作流；每张表均要求完整分页、同名冲突检查、独立确认和完整字段回读。
- M5 项目台账门禁：Bitable `scope.entity=table` 已接入 `make-preview` 和 `validate-preview`，校验台账角色、MCP 就绪字段载荷、业务主键、来源字段和回读计划；既有记录门禁和部分失败分类保持复用。
- M5 跨资源结果门禁：已新增 `validate-project-integration`，按资源验证完整回读、前序依赖、失败与未执行状态，以及来源中观察到的精确目标链接；整体状态只能由资源结果推导为 complete、partial 或 failed。

## 验证结果

- `conda run -n leju python -m unittest discover -s tests -v`：2026-09-13 最新复跑，61 项测试通过。
- `conda run -n leju python scripts/feishu_guard.py validate-defaults config/default-locations.json`：2026-09-12 通过，默认 Wiki 父节点和 Drive 根目录配置有效。
- `conda run -n leju python scripts/feishu_guard.py --help`：命令入口可用。
- `conda run -n leju python /Users/yutaozhang/.codex/skills/.system/skill-creator/scripts/quick_validate.py .`：2026-09-13 最新复跑，`Skill is valid!`。
- `conda run -n leju git diff --check`：2026-09-13 最新复跑，通过。
- `SPEC-M5-01` 元数据核对：Docx 21 个、Sheets 27 个、Bitable 46 个、Wiki 16 个、Drive 52 个工具可见；M5 不引入新传输通道，真实权限与未验收创建接口仍不作可用性承诺。
- `SPEC-M5-02` 只读前向验证：专用测试知识库首页的 5 个直接子节点完整返回；一个 Docx 样例完整回读为版本 9、53 个块，一个 Bitable 样例完整回读为 1 个表、4 个字段和 11 条记录，其中 1 条非空。
- `SPEC-M5-02` Sheets 限制验证：只读取电子表格元数据和 1 个工作表清单；因当前 MCP 无任意区域值读取能力，该来源标记为部分读取，未推断单元格内容。
- `SPEC-M5-02` 来源校验：前向审计包含 4 个来源和 6 条结论，`validate-project-audit` 返回有效并识别 1 个部分来源；全程未调用远端写入工具。
- `SPEC-M5-02` 人工验收：用户于 2026-09-13 明确回复“验收通过”，本 Spec 已完成，并授权 `SPEC-M5-03`。
- `SPEC-M5-03` 本地验证：项目主页、按需文档集、周报内容模型和分资源预览依赖工作流已接入；三项新增门禁测试通过。
- `SPEC-M5-03` 项目主页预览：只读回读确认测试知识库首页有 5 个直接子节点且无同名主页；独立预览 `fs-569955fa94fe16a4` 计划新增 1 个 Docx 项目主页和 32 个内容块，项目预览计划校验通过，未调用远端写入工具。
- `SPEC-M5-03` 项目主页执行：用户确认后，写前状态与本地门禁通过；已创建“M5 项目工作区验收”Docx 子节点并写入 32 个一级内容块。回读确认版本 2、8 个一级标题、父级和内容符合预览，首页原 5 个子节点全部保留。
- `SPEC-M5-03` 文档集预览：项目主页下当前无子节点；已生成独立 PRD 子文档预览 `fs-a7fa0f4a551f5921`，计划新增 33 个内容块和 9 个一级标题，依赖计划校验通过，未执行该写入。
- `SPEC-M5-03` 文档集执行：用户确认后，PRD 预览通过写前状态检查；已创建“M5 项目工作区需求说明”Docx 子节点并写入 33 个内容块。回读确认版本 2、9 个一级标题、标题、父级、顺序和内容与预览一致；项目主页下仅新增该子节点。
- `SPEC-M5-03` 周报预览：已生成独立预览 `fs-dd055fb713518549` 和本地效果确认单，计划在项目主页下新增 1 个 Docx 周报、28 个内容块和 8 个一级标题；三步项目依赖计划校验通过，尚未执行该写入。
- `SPEC-M5-03` 周报执行：用户确认后，周报预览通过写前状态检查；已创建“M5 项目周报（2026-09-07 至 2026-09-13）”Docx 子节点并写入 28 个内容块。回读确认版本 2、8 个一级标题、报告周期、标题、父级、顺序和内容与预览一致；项目主页下仅新增该周报，需求说明保持不变。
- `SPEC-M5-03` 主页链接预览：已生成独立预览 `fs-1cd7fc319ebab458` 和本地效果确认单，只定点替换项目主页“关键文档与链接”章节中的一个占位项目符号，补充已回读的需求说明与周报精确链接；四步项目依赖计划校验通过，尚未执行该修改。
- `SPEC-M5-03` 主页链接执行：用户确认后，预览通过写前版本、完整块顺序和目标文本检查；仅更新一个已声明的项目符号。回读确认项目主页版本由 2 升至 3，总块数仍为 33，全部块 ID、类型和顺序不变；需求说明与周报链接均存在，纯文本无字面量反斜杠换行，两个子文档均保持原位置。
- `SPEC-M5-03` 人工验收：用户于 2026-09-13 明确回复“验收通过，授权 SPEC-M5-04”，本 Spec 已完成。
- `SPEC-M5-04` 本地验证：四类台账的有效建表预览、完整分页、同名冲突、字段契约漂移和回读计划漂移均有单元测试；全量 58 项测试、Skill 校验和 `git diff --check` 通过。
- `SPEC-M5-04` 行动项台账预览：以用户身份完整回读测试 Bitable 应用，当前只有 `Table` 一张数据表且无同名目标；已生成本地效果确认单并绑定预览 `fs-e92a5da44f4ae4f9`，计划新增空表 `M5 行动项` 及 6 个字段，尚未执行远端写入。
- `SPEC-M5-04` 行动项台账执行：用户确认后，预览通过写前完整表清单、指纹和工具门禁；`appTable_create` 成功创建空表 `M5 行动项`。写后完整回读确认应用由 1 张表变为 2 张，原 `Table` 保留；新表 6 个字段的顺序、类型和状态选项与确认单一致，`行动项编号` 为主字段。
- `SPEC-M5-04` 风险台账预览：基于写后完整清单生成独立本地效果确认单和预览 `fs-51b7dd61476be614`，计划新增空表 `M5 风险` 及 8 个字段；尚未执行该写入。
- `SPEC-M5-04` 行动项人工验收：用户检查空表结构后明确回复“验收通过，确认风险台账”。
- `SPEC-M5-04` 风险台账执行：预览 `fs-51b7dd61476be614` 通过写前完整表清单、指纹和工具门禁；`appTable_create` 成功创建空表 `M5 风险`。写后完整回读确认应用由 2 张表变为 3 张，原表保留；新表 8 个字段的顺序、类型及影响、概率、状态选项与确认单一致，`风险编号` 为主字段。
- `SPEC-M5-04` 决策台账预览：基于风险台账写后清单生成独立本地效果确认单和预览 `fs-d6ba5f6da110634b`，计划新增空表 `M5 决策` 及 6 个字段；尚未执行该写入。
- `SPEC-M5-04` 风险人工验收：用户检查风险台账后明确回复“验收通过，确认决策台账”。
- `SPEC-M5-04` 决策台账执行：预览 `fs-d6ba5f6da110634b` 通过写前完整表清单、指纹和工具门禁；`appTable_create` 成功创建空表 `M5 决策`。写后完整回读确认应用由 3 张表变为 4 张，原表保留；新表 6 个字段的顺序和类型与确认单一致，`决策编号` 为主字段。
- `SPEC-M5-04` 指标台账预览：基于决策台账写后清单生成独立本地效果确认单和预览 `fs-86efb9414867f026`，计划新增空表 `M5 指标` 及 6 个字段；尚未执行该写入。
- `SPEC-M5-04` 决策人工验收：用户检查决策台账后明确回复“验收通过，确认指标台账”。
- `SPEC-M5-04` 指标台账执行：预览 `fs-86efb9414867f026` 通过写前完整表清单、指纹和工具门禁；`appTable_create` 成功创建空表 `M5 指标`。写后完整回读确认应用由 4 张表变为 5 张，原表保留；新表 6 个字段的顺序和类型与确认单一致，`指标编号` 为主字段，`当前值` 为数字字段。
- `SPEC-M5-04` 最终人工验收：用户于 2026-09-13 检查指标表后明确回复“验收通过，授权 SPEC-M5-05”，本 Spec 已完成。
- `SPEC-M5-05` 本地验证：跨资源完整结果、部分失败、失败依赖未执行和错误链接均有单元测试；全量 61 项测试、Skill 校验和 `git diff --check` 通过。独立部分失败样例返回 1 个 verified、1 个 failed、1 个 unattempted，整体状态为 `partial`。
- `SPEC-M5-05` 只读基线：项目主页版本 3、共 33 个块，需求说明和周报纯文本可读；Bitable 共 5 张表，原 `Table` 有 4 个字段和 11 条记录（1 条非空），四类 M5 台账字段完整且记录均为 0。
- `SPEC-M5-05` 主页集成执行：用户确认预览 `fs-9f676566545d5a80` 后，写前版本、33 个块顺序、六处原文和本地门禁均通过；6 个定点更新全部成功，版本由 3 升至 9。写后完整回读确认块数量、ID、类型和顺序不变，六处新文本、四张台账直达链接及其余 27 个块均符合预览。
- `SPEC-M5-05` 最终集成验证：项目主页、需求说明、周报及行动项、风险、决策、指标四类台账均完成完整回读；7 个资源全部 verified，9 条精确跨资源链接匹配，`validate-project-integration` 推导整体状态为 `complete`。
- `SPEC-M5-05` 与 M5 最终人工验收：用户于 2026-09-13 检查真实主页结果后明确回复“验收通过”；五个 Spec 与父项退出条件全部满足。
- `SPEC-M3-01` 元数据核对：Sheets 27 个、Bitable 46 个工具；核心参数均支持显式 `useUAT: true`，真实权限和返回结构未验证。
- `SPEC-M3-02` 测试知识库只读检查：以用户身份解析首页并完整列出子节点；仅发现一个无子节点的 `docx` 文档“M2 文档能力集成验收”，未找到 Bitable，零写入。
- `SPEC-M3-02` 测试资源创建：用户确认预览 `fs-68ae5b9616264046` 后，写前状态和本地门禁校验通过；已新增一个空的“M3 多维表格能力验收”Bitable 子节点。回读确认标题、父级和类型正确，首页原子节点保持不变。
- `SPEC-M3-02` Bitable 只读回读：应用元数据可读，默认有 1 个数据表、4 个字段和 10 条空白记录；数据表、字段和记录分页均完整。
- `SPEC-M3-02` 记录创建验证：用户确认预览 `fs-62452ab1c2c99902` 后，写前完整分页和业务主键零匹配检查通过；已新增一条测试记录。回读按业务主键唯一匹配，日期为 2026/09/12，记录总数由 10 增至 11，原 10 条空白记录和 4 个字段均未变化。
- `SPEC-M3-02` 记录更新验证：用户确认预览 `fs-3b5e0a4de8bbbd67` 后，写前完整分页、唯一业务主键和当前日期校验通过；已仅更新目标记录日期。回读按业务主键唯一匹配且日期为 2026/09/13，记录总数仍为 11，原 10 条空白记录和 4 个字段均未变化。
- `SPEC-M3-02` 人工验收：用户于 2026-09-12 检查可见测试结果后明确回复“验收通过”，本 Spec 已完成。
- `SPEC-M3-03` 去重验证：使用真实表格当前状态生成相同业务主键的创建计划，本地门禁以“创建要求业务主键零匹配”拒绝，未调用远端写入。
- `SPEC-M3-03` 幂等验证：对已执行的记录创建预览做重放校验，本地门禁以“该预览已经执行，禁止重复写入”拒绝，未调用远端写入。
- `SPEC-M3-03` 零副作用回读：完整回读仍为 11 条记录，其中 10 条空白记录、1 条目标记录，目标日期保持 2026/09/13，无重复业务主键。
- `SPEC-M3-03` 部分失败验证：确定性分类样例将三个计划业务主键分别归入成功、失败和未执行，且保留原计划顺序；对应单元测试通过。
- `SPEC-M3-03` 人工验收：用户于 2026-09-12 明确回复“验收通过”，本 Spec 已完成。
- `SPEC-M3-04` 本地验证：Sheets 有效预览、范围外地址拒绝、非整格/正则/公式搜索拒绝、工作表不唯一拒绝、查找不完整拒绝、替换参数篡改拒绝和写后验证篡改拒绝均有单元测试并通过。
- `SPEC-M3-04` 测试资源只读检查：专用知识库首页完整列出 2 个子节点，仅有 M2 文档和 M3 多维表格，无 Sheet；零写入。
- `SPEC-M3-04` 能力缺口复核：Sheets 创建接口只能创建空表；Drive 文件上传缺少分片上传工具，因此无法通过飞书 MCP 自行导入带数据的 xlsx，且未尝试浏览器、OpenAPI 或其他连接器。
- `SPEC-M3-04` 测试资源创建：用户确认预览 `fs-0e3bf9ed7b272c50` 后，写前状态与本地门禁校验通过；已在专用知识库首页下创建空白“M3 电子表格能力验收”Sheet。
- `SPEC-M3-04` Sheet 回读：知识库节点标题、父级和 `sheet` 类型正确；电子表格标题与 Token 一致，默认工作表为 `Sheet1`（ID `6203dd`，200 行 × 20 列）。
- `SPEC-M4-01` 元数据核对：Wiki 16 个工具覆盖空间、节点和成员；Drive 52 个工具覆盖清单、元数据、复制、移动、版本、评论、权限和异步导入导出；Minutes 3 个工具仅覆盖基础信息、统计和媒体。
- `SPEC-M4-01` 缺口核对：Drive 上传缺少分片上传工具，Minutes 缺少列表、搜索和文字稿工具；这些能力不进入当前路线图，未调用远端资源。
- `SPEC-M4-02` Wiki 只读前向验证：默认父节点以用户身份解析为 `docx`，所属私有知识空间可读；3 个直接子节点在终止页完整返回，类型分别为 `docx`、`bitable` 和 `sheet`。
- `SPEC-M4-02` Drive 只读前向验证：默认根目录返回 41 个直接子项且 `has_more` 为 false；按一个精确文件夹 token/type 回读到唯一元数据，请求对可追溯。
- `SPEC-M4-02` 权限审计验证：测试知识库首页的容器协作者与公开设置均以用户身份读取成功；外部访问关闭、链接范围为组织内可读。全程未调用写入工具。
- `SPEC-M4-02` 人工验收：用户于 2026-09-12 明确回复“验收通过”，并授权 `SPEC-M4-03`。
- `SPEC-M4-03` 本地预览接入：已完成 Wiki/Drive 写入预览的本地门禁与单测样例覆盖（create/copy/move/update、同名冲突、空操作与无效源）。
- `SPEC-M4-03` Wiki 创建执行：用户确认预览 `fs-96476de4a0849a12` 后，写前状态指纹与本地门禁校验通过；已在专用测试知识库首页下创建空白“M4 Wiki 写入能力验收”文档节点。
- `SPEC-M4-03` Wiki 创建回读：新节点标题、父级和 `docx` 类型与预览一致；首页直接子节点由 3 个增至 4 个，原有三个子节点均保持不变。用户已检查可见结果并明确确认本步验收通过。
- `SPEC-M4-03` Wiki 改名验证：真实 MCP 元数据确认改名工具为 `wiki_v2_spaceNode_updateTitle`，本地测试常量已同步修正；用户确认预览 `fs-60db23cf440e5c1d` 后，写前指纹校验通过并完成标题更新。回读确认节点 Token、父级和 `docx` 类型不变，其他三个首页子节点未变化；用户已明确确认本步验收通过。
- `SPEC-M4-03` Wiki 复制验证：用户确认预览 `fs-ec88065472f1a906` 后，写前指纹校验通过；已在测试知识库首页创建“M4 Wiki 复制能力验收”副本。回读确认副本为新的 `docx` 节点，首页直接子节点由 4 个增至 5 个，源节点标题和位置不变；用户已明确确认本步验收通过。
- `SPEC-M4-03` Wiki 移动验证：用户确认预览 `fs-55e008cbc88a46c5` 后，写前指纹校验通过；已将“M4 Wiki 复制能力验收”移入“M4 Wiki 改名能力验收”节点下。回读确认副本从首页移出并成为目标节点唯一子节点，标题、Token 和 `docx` 类型不变；用户已明确确认本步验收通过。
- `SPEC-M4-03` Drive 创建验证：真实 MCP 元数据确认文件夹创建、文件夹移动和文档版本工具名分别为 `file_createFolder`、`file_move` 和 `fileVersion_create`，本地测试常量已同步；门禁允许既有空标题条目但仍拒绝空的新名称。用户确认预览 `fs-a11173a5b6855762` 后，写前指纹校验通过并在根目录创建“M4 Drive 写入能力验收”文件夹；目录清单和精确元数据回读确认根目录由 41 项增至 42 项，新资源名称和 `folder` 类型正确，原有 41 个 Token 全部保留。用户已明确确认本步验收通过。
- `SPEC-M4-03` Drive 复制验证：按真实 `file_copy` 契约将复制与移动源类型、源父目录要求拆分；复制拒绝文件夹，支持精确 `docx` Token 且不虚构源父目录。用户确认预览 `fs-0b3cc6554dfb4866` 后，写前指纹校验通过并将 Wiki 测试文档复制到测试文件夹。回读确认目标目录只有一个新的“M4 Drive 复制能力验收”`docx`，目录清单与精确元数据一致；源文档 Token、标题和修改时间不变。用户已明确确认本步验收通过。
- `SPEC-M4-03` Drive 移动首次执行：用户确认预览 `fs-8fa9420e11191384` 后，写前指纹校验通过，但以空字符串作为写入目标根目录时 `file_move` 返回 `params error`。未重试旧预览；回读确认文件仍在测试文件夹、根目录仍为 42 项且文件元数据不变，零远端副作用。
- `SPEC-M4-03` Drive 根目录修正执行：用户确认预览 `fs-160fcfc0198e59ce` 后，写前状态与本地门禁校验通过；已使用完整根目录清单中唯一一致的实际 `parent_token` 完成移动，旧预览继续禁止重放。回读确认测试文件夹已为空，根目录由 42 项增至 43 项，目标文件的 Token、标题、类型和元数据均未变化；等待用户人工验收。
- `SPEC-M4-03` Drive 移动人工验收：用户于 2026-09-13 检查可见结果后明确回复“本步验收通过，继续”。
- `SPEC-M4-03` Drive 版本模型修正：已按真实 `fileVersion_create` 契约改为在同一 `docx` 或 `sheet` 上创建命名版本，锁定文件 Token、版本名、对象类型与 `useUAT: true`；拒绝不完整版本清单、同名版本和调用参数篡改。46 项单元测试与 `git diff --check` 均通过。
- `SPEC-M4-03` Drive 版本创建预览：以用户身份回读目标文档元数据和完整版本列表，当前无已有版本；已生成预览 `fs-644d4a527de61646`，等待用户确认，尚未调用版本创建工具。
- 用户可视预览规则：用户于 2026-09-13 明确要求预览必须能在本地直接查看实际写入效果；已将 `preview_id` 收敛为内部绑定编号，并在产品决策、产品验收、Skill 入口和预览工作流中要求每次写入前展示本地可视化效果确认单。
- Drive 版本本地效果确认单：已展示版本创建前后数量、实际提交字段及正文、标题、位置、权限不变范围；尚未执行远端写入。
- `SPEC-M4-03` Drive 版本创建执行：用户确认本地效果确认单后，写前元数据、完整版本列表、状态指纹和本地门禁均通过；版本创建成功。写后完整列表由 0 项变为 1 项，版本详情的名称、`docx` 类型和源文档关联均与确认单一致；异步处理随后更新了源文档修改时间，但标题和位置未变化。
- `SPEC-M4-03` Drive 版本创建人工验收：用户于 2026-09-13 在原文档的版本历史中检查命名版本后明确回复“本步验收通过，继续”。
- `SPEC-M4-03` 文档入库模型：已接入将现有 Drive 文档移动到指定 Wiki 父节点的预览门禁，锁定源文档、原目录、目标空间、目标父节点、`apply: false` 和用户身份，并拒绝同名冲突及参数篡改。
- `SPEC-M4-03` 文档入库预览：完整读取 Drive 根目录、源文档元数据和测试知识库首页子节点，确认源文档位于根目录且目标无同名节点；已生成本地效果确认单并绑定预览 `fs-ccd183fe1469ff1f`，尚未执行远端移动。
- `SPEC-M4-03` 文档入库执行：用户确认本地效果确认单后，写前状态指纹与本地门禁通过；异步移动任务返回成功。回读确认文档从 Drive 根目录移出，测试知识库首页直接子节点由 4 个增至 5 个，新节点标题、`docx` 类型和原文档关联一致，已创建的命名版本仍保留；等待用户人工验收。
- `SPEC-M4-03` 文档入库人工验收：用户于 2026-09-13 检查知识库中的目标节点后明确回复“本步验收通过，继续”。
- `SPEC-M4-03` Drive 导出模型：已接入 `doc`/`docx` 到 `docx`/`pdf`、`sheet`/`bitable` 到 `xlsx` 的异步导出预览门禁，锁定源 Token、类型、扩展名和用户身份；当前不宣称 CSV 或本地下载能力。
- `SPEC-M4-03` Drive 导出预览：回读确认测试文档仍位于测试知识库且元数据稳定；已生成 Word 导出的本地效果确认单并绑定预览 `fs-85133a43c1448824`，尚未创建远端导出任务。
- `SPEC-M4-03` Drive 导出任务执行：用户确认本地效果确认单后，写前状态指纹和本地门禁通过；异步任务返回成功及非空文件 Token、正确文件名、扩展名和 3,343 字节大小，源文档标题、知识库位置和修改时间未变化。由于当前 MCP 无导出文件下载工具，未验证文件可下载、可打开或内容完整；此前“可验证导出已成功”的汇报不准确，已更正，不请求将完整导出能力验收为通过。
- `SPEC-M4-03` Drive 现有文件 Token 导入模型：已接入 `.docx` 文件 Token 到指定非根目录 `docx` 的预览门禁，锁定源 Token、扩展名、正文件大小、目标标题、目标文件夹、挂载类型和用户身份，并拒绝同名冲突及参数篡改。
- `SPEC-M4-03` Drive 导入尝试：用户确认本地效果确认单后，写前重新读取导出任务、空测试文件夹和源文档，并通过预览 `fs-3ee903117afb4aaa` 的指纹与参数门禁。`importTask_create` 返回 `1069910 import file extension not match`，未创建异步任务。写后回读确认目标文件夹仍为空，源文档仍为版本 2 且正文未变化；该预览已消耗，不能重放。
- `SPEC-M4-03` Drive 导入边界修正：导出任务结果虽然报告 `docx` 扩展名和文件 Token，但该 Token 属于下载产物，不能作为已验证的导入源。导入门禁已增加来源校验并拒绝 `export_task`；当前 MCP 又缺少上传分片工具，因此尚无可用于前向验收的上传文件 Token。
- `SPEC-M4-03` Drive 导入新来源：用户已按原格式上传一个普通 `.docx` 文件；Drive 完整清单确认类型为 `file`、文件名保留 `.docx` 后缀且位于测试文件夹。当前元数据工具不返回普通文件大小，门禁已允许“精确现有 Drive 文件且大小未知”的来源，同时仍要求普通文件类型、后缀、来源和目标清单一致。
- `SPEC-M4-03` Drive 普通文件导入预览：已锁定普通源文件、目标测试文件夹和新增标题，生成本地效果确认单并绑定预览 `fs-56774e3d04d5d41c`。当前文件夹有 1 个普通源文件；确认后拟新增一个“M4 Drive 导入能力验收”在线文档，源文件保持不变。MCP 无法预读普通 Word 正文，已在确认单中提供精确源文件链接并披露该限制。
- `SPEC-M4-03` Drive 普通文件导入执行：用户确认本地效果确认单后，写前重新读取普通源文件与目标文件夹并通过预览 `fs-56774e3d04d5d41c` 的指纹、来源和参数门禁；异步导入任务返回成功。写后完整回读确认文件夹由 1 项变为 2 项，普通源文件的 Token、类型、名称和修改时间保持不变；新增在线文档标题、`docx` 类型和目标位置与预览一致，版本 2、正文非空，并保留一个 3 行 × 4 列表格及 12 个单元格。由于 MCP 无法预读普通 Word 正文，未进行导入前后逐字或格式自动比较，等待用户人工验收。
- `SPEC-M4-03` Drive 普通文件导入人工验收：用户于 2026-09-13 检查真实在线文档后明确回复“本步验收通过，继续”；`SPEC-M4-03` 的全部范围已完成。
- `SPEC-M4-04` 历史验证：曾从用户提供的文档中定位到关联妙记；基础信息、统计和媒体三个用户身份调用均返回 `99991679`，未取得真实妙记数据，也未判定验证成功。该能力现已暂缓。
- `AGENTS.md` 与 `.ai/STATUS.md` 结构和空白格式检查：通过。
- Issue/Spec 开发门禁已写入 `AGENTS.md`；已创建首个产品需求 Spec，尚未创建本地 Issue。
- `SPEC-M1B-01` 的能力数量、必需工具和授权边界已完成静态核对；未调用真实飞书资源。
- `SPEC-M1B-01` 已由用户确认；`SPEC-M1B-02` 已获只读授权，已完成一次用户身份读取。
- 已通过 `useUAT: true` 解析 Wiki 节点并读取其挂载新版文档的基本信息与全部块。
- 预览 `fs-e25f22ab13739ae6` 经用户确认后通过写前指纹校验；局部更新返回文档版本 `4`。
- 写后分别回读文档信息、目标块、完整块列表和纯文本：标题保持“TEST”，目标正文为“M1-B 集成测试”，未发现非目标变化。
- `SPEC-M2-01` 真实只读演示：用户指定的测试知识库首页解析为新版文档，版本 1，共回读 12 个块且无分页遗漏；识别默认占位内容和两个尾部空文本块，未调用写入工具。
- `SPEC-M2-01` 已于 2026-09-12 通过用户人工验收。
- `SPEC-M2-02` 真实目标预览：6 个所需飞书 MCP 工具均可见；读取测试知识库父节点及空子节点列表后，生成 20 块技术方案创建预览 `fs-0a6b89e0d76b3550`，未调用写入工具。
- `SPEC-M2-02` 已于 2026-09-12 通过用户人工验收；用户同时明确授权 `SPEC-M2-03`。
- `SPEC-M2-03` 真实只读演示：以用户身份解析指定测试知识库首页并读取版本 2 的全部 12 个块；唯一定位“🎯  愿景和目标”章节，生成追加二级标题和轻量表格的预览 `fs-c7b4c56f60e56146`，未调用写入工具。
- `SPEC-M2-03` 已于 2026-09-12 通过用户人工验收；用户同时明确授权 `SPEC-M2-04` 的只读阶段。
- `SPEC-M2-04` 能力检查：创建和回读所需的 7 个飞书 MCP 工具均可见。
- `SPEC-M2-04` 只读检查：以用户身份读取指定测试知识库父节点和空子节点列表，并回读首页版本 2 的全部 12 个块；未调用写入工具。
- `SPEC-M2-04` 只读审计：首页仍为默认占位模板，四个一级章节均是说明性占位内容，尾部有两个空文本块；所有块均可读，未发现未知块。
- `SPEC-M2-04` 创建预览：生成 35 块技术方案预览 `fs-2b51f1ee7f986cb3`，拟在测试知识库首页下创建“M2 文档能力集成验收”；未调用写入工具。
- `SPEC-M2-04` 创建执行：预览 `fs-2b51f1ee7f986cb3` 经确认并通过写前校验；子文档节点创建成功，首个正文写入请求因 `invalid param` 失败。
- `SPEC-M2-04` 部分失败回读：新文档标题和父级正确、版本为 1，正文仅含页面根块；未盲目重试，旧预览已部分执行且不可重放。
- `SPEC-M2-04` 修正预览执行：预览 `fs-724a86454cffadfb` 经用户确认并通过写前指纹、完整性和工具校验；转换结果为 35 个一级块、71 个总块、8 个一级标题、5 个二级标题和 2 张表格。
- `SPEC-M2-04` 第二次正文失败：嵌套块单次写入仍返回 `invalid param`；已停止且未重试，回读确认文档仍为版本 1、仅有页面根块。
- `SPEC-M2-04` 幂等验证：将 `fs-724a86454cffadfb` 加入已执行集合后，本地写前校验以“该预览已经执行，禁止重复写入”拒绝重放。
- `SPEC-M2-04` 失败原因检查：首次手工表格载荷缺少单元格后代且 `merge_info` 为空，可确定为无效载荷；第二次调用的文档、根块、版本和用户身份参数一致且未返回权限错误，转换块图的 ID 和父子引用自洽，错误集中在当前 MCP 的嵌套块请求参数契约。服务端只返回通用 `invalid param`，无法从现有只读证据定位到单一字段。
- `SPEC-M2-04` 预览撤回：两批嵌套块写入预览 `fs-758414bf2ee3c94c` 只改变请求数量，未隔离参数形状，诊断后认定依据不足并撤回，禁止执行。
- `SPEC-M2-04` 最小载荷预览：回读确认子文档版本 1 且只有页面根块；Markdown 转换得到单个普通文本块后，生成预览 `fs-9f28e3944942c766`，拟通过 `documentBlockChildren_create` 在根块下新增一行“M2 正文写入链路验证”。
- 高层 `docx_builtin_import` 只能导入并新建文档，不能更新当前已存在的 Wiki 子文档，因此未用于修复。
- `SPEC-M2-04` 基础写入验证：预览 `fs-9f28e3944942c766` 经确认并通过写前校验；`documentBlockChildren_create` 成功创建普通文本块，文档版本由 1 升至 2。回读得到根块和新增文本块共 2 块，正文与预览一致，确认目标、用户权限和基础子块参数正常。
- `SPEC-M2-04` 嵌套隔离预览：转换单张 2×2 Markdown 表格得到 1 个一级表格块和 8 个后代块，并生成预览 `fs-1f29e5d317aded00`。
- `SPEC-M2-04` 嵌套隔离执行：预览 `fs-1f29e5d317aded00` 经确认并通过写前校验，但实际提交的 `merge_info` 比预览多一项，服务端返回 `invalid param`。已停止且未重试；回读确认文档仍为版本 2、只有根块和已验证文本块，无副作用。该预览已消耗。
- `SPEC-M2-04` 嵌套修正预览：重新转换同一张 2×2 表格，得到 1 个一级表格块、8 个后代块和严格匹配四个单元格的 4 项 `merge_info`，并生成预览 `fs-252c67c5acf657fe`。
- `SPEC-M2-04` 瞬态载荷门禁：确认 `fs-252c67c5acf657fe` 后，写前阶段发现会话内转换载荷未跨用户回合保留，未调用写入工具并使该预览失效。
- `SPEC-M2-04` 持久化修正预览：再次转换同一张 2×2 表格，得到 1 个一级表格块、8 个后代块和 4 项 `merge_info`；预览 `fs-ab99b19024daf79c` 的完整非敏感执行载荷已保存到任务本地临时文件并回读核对。
- `SPEC-M2-04` 嵌套接口结论：预览 `fs-ab99b19024daf79c` 经确认、写前指纹校验和临时载荷回读后原样执行，`documentBlockDescendant_create` 仍返回 `invalid param`。写后回读确认文档版本仍为 2、正文和块清单无变化；该预览已消耗，临时载荷已删除。
- `SPEC-M2-04` 分步表格预览：已停用当前失败的嵌套接口，生成空 2×2 表格创建预览 `fs-a142a769ba61f1c6`；拟通过已验证的 `documentBlockChildren_create` 创建表格容器并回读单元格 ID，填充单元格将使用后续独立预览。
- `SPEC-M2-04` 空表创建验证：预览 `fs-a142a769ba61f1c6` 经确认并通过写前校验；`documentBlockChildren_create` 成功创建 2×2 表格，文档版本由 2 升至 3。回读确认根块新增一个表格子块，飞书生成四个单元格及四个默认空文本块，原验证文本未变化。
- `SPEC-M2-04` 单元格填充预览：已将四个空文本块按表格行列映射到“检查项 / 状态 / 普通文本写入 / 已通过”，并生成、持久化四次定点 `update_text_elements` 预览 `fs-308f45cd9e1827fe`。
- `SPEC-M2-04` 单元格填充验证：预览 `fs-308f45cd9e1827fe` 经确认并通过写前校验；四个定点更新全部成功，文档版本由 3 依次升至 7。回读共 11 个块，表格按行列顺序为“检查项 / 状态 / 普通文本写入 / 已通过”，原验证文本未变化，临时载荷已删除。
- `SPEC-M2-04` 分步表格人工验收：用户于 2026-09-12 检查测试子文档后明确回复“验收通过”；普通正文和分步表格写入能力正式验收通过。
- `SPEC-M2-04` 剩余阶段授权：用户于 2026-09-12 明确授权继续完整正文、章节编辑与幂等复核；每次远端写入仍须单独生成预览并确认。
- `SPEC-M2-04` 完整正文追加预览：回读确认目标文档当前为版本 7、共 11 个块；Markdown 只读转换得到 41 个扁平一级块（8 个一级标题、5 个二级标题、27 个项目符号、1 个引用），已生成预览 `fs-ddbcd33e95d640f8` 并持久化精确非敏感载荷，尚未执行远端写入。
- `SPEC-M2-04` 完整正文追加验证：预览 `fs-ddbcd33e95d640f8` 经确认并通过写前指纹、完整性和工具校验；写入后文档版本由 7 升至 8、总块数由 11 增至 52，41 个新增块的顺序、内容、类型和根块父级均与预览一致，原普通文本与表格块保持不变，纯文本不存在字面量反斜杠换行。
- `SPEC-M2-04` 完整正文幂等复核：将 `fs-ddbcd33e95d640f8` 加入已执行集合后，本地写前校验以“该预览已经执行，禁止重复写入”拒绝重放，未产生第二次远端调用。
- `SPEC-M2-04` 章节定点编辑预览：在版本 8、共 52 个块的完整文档中唯一定位“验证与验收”一级章节，确定前后边界为“兼容性、迁移与回滚”和“风险”，生成在章节末尾追加 1 个验收记录项目符号的预览 `fs-5353d84cae8f848b`；精确非敏感载荷已持久化，尚未执行远端写入。
- `SPEC-M2-04` 章节定点编辑验证：预览 `fs-5353d84cae8f848b` 经确认并通过写前校验；文档版本由 8 升至 9、总块数由 52 增至 53，新项目符号准确位于“验证与验收”末尾和“风险”之前，47 个章节外既有块及全部原块保持不变。
- `SPEC-M2-04` 章节编辑幂等复核：将 `fs-5353d84cae8f848b` 加入已执行集合后，本地写前校验拒绝重放，未产生第二次远端调用。
- `SPEC-M2-04` 最终只读审计：仅调用文档信息、全量块和纯文本读取工具，零写入；版本 9 共 53 个块，8 个一级标题、5 个二级标题，无重复标题、跳级、空章节、未知块或字面量反斜杠换行。唯一低风险提示是测试文本与表格位于方案摘要之前，该位置为保留集成证据而有意保留。
- `SPEC-M2-04` 与 M2 最终验收：用户于 2026-09-12 明确回复“验收通过”，M2 的四个 Spec 及父项退出条件全部满足。

## 未完成与阻塞项

- 当前 Sheets MCP 没有任意区域值读取和写入工具；FR-S02 至 FR-S04 已移出当前范围，不作为阻塞项，且不得通过扩展 MCP、其他连接器、浏览器或 OpenAPI 补齐。
- `SPEC-M3-04` 尚缺最小样例数据、真实替换回读和用户人工验收；当前 MCP 无法自行写入初始样例单元格。
- M3 的 `SPEC-M3-04` 仍受最小样例数据不足阻塞；妙记功能已移出当前路线图，不再作为阻塞项。
- `SPEC-M2-01` 已验证标题、引用和文本块的真实读取与审计；列表、表格、代码、媒体、嵌套容器和未知块仍缺少真实样例验证。
- 后续任何真实写入仍必须先生成独立预览，再由用户确认对应的 `preview_id`。
- 复杂媒体、Callout、分栏、代码块和大型表格仍未进行真实样例验证，不影响本 Spec 已声明的基础文档能力范围。

## 下一步

M5 已完成；下一开发里程碑 M6 需要用户明确授权。M3 的 `SPEC-M3-04` 仍需样例数据到位后恢复验收。

## 维护说明

- 本文件保存当前状态，不充当历史日志；旧结论失效后应直接更新。
- 里程碑定义和退出条件以 `.ai/ROADMAP.md` 为准，稳定产品决策以 `.ai/DECISIONS.md` 为准。
- 每次仓库状态发生实质变化时，负责该变更的 AI 必须在结束前同步更新本文件。
- 如果本文件与 Git、实现或最新测试证据不一致，先核验事实，再修正本文件。
