# SPEC-M2-03：章节编辑、排版与文档内表格

- 父项类型：产品需求
- 父项 ID：`M2`
- 状态：已完成
- 用户授权：已授权（2026-09-12）
- 执行顺序：3 / 4
- 更新时间：2026-09-12

## 目标

实现现有文档的章节级追加、插入和替换，以及标题、列表、引用、Callout、分栏和轻量表格的安全规划与执行规则。

## 范围

- 实现 FR-T04、FR-D03 至 FR-D05，并在编辑中落实 FR-T06 至 FR-T08。
- 使用 `SPEC-M2-01` 的块 ID、标题路径和章节边界定位范围。
- 预览完整展示目标块、前后差异、结构变化、格式风险和非目标保护方法。
- 定义不支持块、格式降级、重复追加和部分失败的处理规则。

## 非目标

- 不实现图片、附件、评论、版本或高风险权限操作。
- 不在没有确认预览时执行写入。
- 不完成真实 MCP 集成验收。

## 依赖

- `SPEC-M2-02` 已完成并经用户确认。
- M1 的预览、冲突检测、幂等和回读验证规则保持生效。

## 交付物

- 文档编辑与排版参考文件和工作流。
- 章节范围、格式降级和未知块保护的本地检查。
- 对应行为测试。

## 验证与验收

- 同名章节无法唯一定位时停止。
- 编辑计划不会隐式覆盖未知块或章节外内容。
- 同一追加预览重复执行不会产生重复段落。
- 格式无法保真时在预览中明确标记。
- 全量单元测试、Skill 校验和 `git diff --check` 通过。
- 已使用指定测试知识库首页的“🎯  愿景和目标”章节生成真实只读追加预览 `fs-c7b4c56f60e56146`；定位结果唯一，计划追加二级标题和轻量表格，未调用写入工具。
- 真实效果已向用户展示，并于 2026-09-12 获得人工验收。

## 下一门禁

用户已于 2026-09-12 人工验收，并明确授权 `SPEC-M2-04` 的只读阶段；每次真实写入仍需另行确认预览。
