# SPEC-M5-03：项目主页、文档集与周报

- 父项类型：产品需求
- 父项 ID：`M5`
- 状态：已完成
- 用户授权：已授权（2026-09-13，“验收通过，授权 SPEC-M5-03”）
- 执行顺序：3 / 5
- 更新时间：2026-09-13

## 目标

实现项目主页、项目文档集和周报的内容模型，以及按资源拆分的本地效果预览。

## 范围

- FR-P01、FR-P02、FR-P07。
- Wiki 目录、Docx 项目主页与文档模板、来源链接和状态汇总。

## 非目标

- 不实现结构化行动项、风险、决策或指标台账。
- 不用一次确认授权多个资源写入。

## 依赖与验收

- 依赖 `SPEC-M5-02` 完成、获用户验收并明确授权本 Spec。
- 本地预览和门禁测试通过，并完成用户可见前向测试与人工验收。

## 当前验证结果

- 已新增项目工作区文档模型与预览工作流；项目主页同时作为 Wiki 索引和 Docx 子文档父节点，不额外创建空目录。
- 已扩展项目主页和周报模板，要求状态日期、报告周期、精确来源链接和具体待确认项；项目文档集只创建用户声明的子集。
- 已新增 `validate-project-preview-plan`，校验每个资源使用独立预览、依赖只指向前序步骤、新建资源链接必须依赖已完成资源，以及周报周期有效。
- 55 项单元测试、Skill 校验和 `git diff --check` 已通过。
- 用户确认项目主页效果单后，预览 `fs-569955fa94fe16a4` 通过写前指纹和工具门禁；已创建“M5 项目工作区验收”Docx 子节点并写入 32 个内容块。
- 回读确认新文档版本为 2、包含 8 个一级标题，标题、父级、顺序和内容与预览一致；测试知识库首页原 5 个子节点全部保留。
- 用户确认 PRD 效果单后，预览 `fs-a7fa0f4a551f5921` 通过写前状态检查；已创建“M5 项目工作区需求说明”Docx 子节点并写入 33 个内容块。
- 回读确认需求说明版本为 2、包含 9 个一级标题，标题、父级、顺序和内容与预览一致；项目主页下仅新增该子节点。
- 已生成周报独立预览 `fs-dd055fb713518549` 与本地效果确认单；项目依赖计划按 `STEP-001`、`STEP-002`、`STEP-003` 校验通过，尚未执行周报写入。
- 用户确认周报效果单后，预览 `fs-dd055fb713518549` 通过写前状态检查；已创建“M5 项目周报（2026-09-07 至 2026-09-13）”Docx 子节点并写入 28 个内容块。
- 回读确认周报版本为 2、包含 8 个一级标题，标题、父级、报告周期、顺序和内容与预览一致；项目主页下仅新增该周报，既有需求说明保持不变。
- 已生成项目主页链接补充预览 `fs-1cd7fc319ebab458` 和本地效果确认单，只定点替换“关键文档与链接”章节中的一个占位项目符号；四步依赖计划校验通过，尚未执行主页修改。
- 用户确认主页链接补充效果单后，预览 `fs-1cd7fc319ebab458` 通过写前版本、完整块顺序和目标文本检查；仅更新已声明的一个项目符号。
- 回读确认项目主页版本由 2 升至 3，总块数仍为 33，全部块 ID、类型和顺序不变；需求说明与周报链接均存在，纯文本无字面量反斜杠换行，两个子文档均保持原位置。
- 用户于 2026-09-13 检查真实页面后明确回复“验收通过，授权 SPEC-M5-04”；本 Spec 已完成。
