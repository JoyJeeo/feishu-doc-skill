# SPEC-M5-02：项目材料与只读审计

- 父项类型：产品需求
- 父项 ID：`M5`
- 状态：已完成
- 用户授权：已授权（2026-09-13，“确认 SPEC-M5-01，授权 SPEC-M5-02”）
- 执行顺序：2 / 5
- 更新时间：2026-09-13

## 目标

建立项目材料清单、来源追踪和只读项目审计模型，让结论可追溯且不混淆事实、推论与建议。

## 范围

- FR-T09、FR-P08。
- Docx、Wiki、Drive 和 Bitable 的项目材料读取、完整性标记、来源引用、状态冲突和缺失项审计。

## 非目标

- 不修改任何飞书资源，不生成项目主页或台账写入预览。
- 不从 Sheets 读取任意单元格值，不读取妙记。

## 依赖与验收

- 依赖 `SPEC-M5-01` 完成、获用户确认并明确授权本 Spec。
- 本地模型与测试通过，并在专用测试知识库完成只读前向验收和用户人工验收。

## 当前验证结果

- 已新增项目审计模型、只读工作流和 Skill 路由；本地校验要求事实、发现与建议引用已登记来源，部分读取必须披露限制。
- 专用测试知识库首页的 5 个直接子节点在终止页完整返回，类型为 3 个 Docx、1 个 Bitable 和 1 个 Sheet。
- 一个 Docx 样例完整回读为版本 9、53 个块；一个 Bitable 样例完整回读为 1 个表、4 个字段和 11 条记录，其中 1 条非空。
- Sheet 只读取元数据和 1 个工作表清单；当前 MCP 无任意区域值读取能力，已作为部分来源限制，不推断单元格内容。
- 前向审计证据映射包含 4 个来源和 6 条结论，本地 `validate-project-audit` 校验通过；全程只读，未调用远端写入工具。
- 52 项单元测试、Skill 校验和 `git diff --check` 已通过。
- 用户于 2026-09-13 明确回复“验收通过”，本 Spec 已完成，并授权 `SPEC-M5-03`。
