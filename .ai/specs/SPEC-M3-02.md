# SPEC-M3-02：多维表格读取与预览模型

- 父项类型：产品需求
- 父项 ID：`M3`
- 状态：已完成（本地验证、真实前向测试和用户人工验收均通过）
- 用户授权：已授权（2026-09-12，“授权 SPEC-M3-02”）
- 执行顺序：2 / 5
- 更新时间：2026-09-12

## 目标

建立 Bitable 表、字段、记录、业务主键和数据质量的统一读取与预览模型，并用确定性门禁保护创建和更新范围。

## 范围

- FR-B01 至 FR-B05、FR-B09 的本地模型与工作流规则。
- 表和字段完整分页、字段类型校验、记录匹配与主键去重。
- 单条和批量记录创建、更新的差异预览、部分失败与幂等规则。

## 非目标

- 不执行专用测试知识库之外的真实飞书写入。
- 不实现视图、表单或工作流管理。
- 不实施任何 Sheets 能力。

## 依赖

- `SPEC-M3-01` 完成并获用户确认。
- 用户明确授权本 Spec。

## 交付物

- `references/bitable/data-model.md`
- `workflows/bitable-record-preview.md`
- `SKILL.md`、`workflows/analyze.md` 与 `workflows/preview.md` 的 Bitable 路由
- `scripts/feishu_guard.py` 的 Bitable 记录创建与更新门禁
- `tests/test_feishu_guard.py` 的 Bitable 安全行为测试

## 验证与验收标准

- 完整分页后才能判断业务主键是否重复。
- 创建要求主键零匹配，更新要求唯一匹配。
- 字段值与字段类型一致；预览列出记录选择规则、数量和逐字段差异。
- 全量本地测试、Skill 结构校验和 `git diff --check` 通过。
- 保持“进行中”直至用户完成真实前向验收。

## 当前验证

- 31 项单元测试通过，覆盖主键零匹配创建、唯一匹配更新、完整分页、字段类型、只读字段、选项保护和字段差异一致性。
- Skill Creator 结构校验通过。
- `git diff --check` 通过。
- 已成功创建并回读真实测试 Bitable；记录写入前向测试和人工验收待完成。
- 已使用用户身份只读解析专用测试知识库首页并完整列出子节点：首页下仅有一个 `docx` 子节点“M2 文档能力集成验收”，该节点无子节点，未找到 Bitable 测试资源。
- 本次只读检查未调用任何远端写入工具，也未尝试其他通道。
- 用户确认预览 `fs-68ae5b9616264046` 后已创建空 Bitable 子节点；回读确认标题、父级和类型正确，首页原子节点保持不变。
- Bitable 元数据回读成功；默认包含 1 个数据表、4 个字段和 10 条空白记录，字段与记录分页均完整。
- 用户确认预览 `fs-62452ab1c2c99902` 后已创建业务主键为“M3-验收-001”的测试记录；回读唯一匹配，日期为 2026/09/12，记录总数由 10 增至 11，原空白记录和字段定义未变化。
- 用户确认预览 `fs-3b5e0a4de8bbbd67` 后已仅将该测试记录日期从 2026/09/12 更新为 2026/09/13；回读按业务主键唯一匹配，记录总数、原空白记录和字段定义均未变化。
- 用户于 2026-09-12 检查可见结果后明确回复“验收通过”，本 Spec 完成。

## 下一门禁

等待用户明确授权 `SPEC-M3-03`；未经授权不实施后续集成验收。
