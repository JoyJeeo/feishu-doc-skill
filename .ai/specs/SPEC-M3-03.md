# SPEC-M3-03：多维表格真实集成验收

- 父项类型：产品需求
- 父项 ID：`M3`
- 状态：已完成
- 用户授权：已授权（2026-09-12，“授权 SPEC-M3-03”）
- 执行顺序：3 / 5
- 更新时间：2026-09-12

## 目标

使用专用测试资源验证 Bitable 读取、字段校验、业务主键去重、创建、更新、批量结果和幂等保护。

## 范围

- 先只读检查测试多维表格，再逐次生成并确认真实写入预览。
- 回读验证目标记录和非目标记录，复核重复预览保护。

## 非目标

- 不操作未指定资源，不删除记录，不使用应用身份或替代通道。

## 依赖

- `SPEC-M3-02` 完成并获用户验收。
- 用户提供或指定测试 Bitable；每次写入单独确认 `preview_id`。

## 当前依据

- 专用测试 Bitable 已由 `SPEC-M3-02` 创建并获用户人工验收。
- 单记录创建与更新的真实 MCP 调用、回读和非目标保护已有证据，可直接复用。
- 本 Spec 待补重复业务主键拒绝、已执行预览拒绝重放和部分失败分类证据。

## 交付物与验收标准

- 真实 MCP 调用证据、用户可检查的结果和人工验收记录。
- 重复业务主键不产生新记录；唯一目标只更新预览字段。
- 部分失败准确区分成功、失败和未执行项。

## 当前验证

- 复用 `SPEC-M3-02` 的真实创建与更新证据：目标记录按业务主键唯一匹配，更新只改变预览日期字段。
- 使用真实表格当前状态生成同业务主键创建计划，本地门禁以“创建要求业务主键零匹配”拒绝，未调用写入工具。
- 对已执行创建预览做重放校验，本地门禁以“该预览已经执行，禁止重复写入”拒绝，未调用写入工具。
- 写后只读复核仍为 11 条记录，其中 10 条空白记录、1 条目标记录，未产生重复项。
- 新增确定性批量结果分类，按计划顺序输出成功、失败和未执行业务主键；32 项单元测试、Skill 校验和 `git diff --check` 通过。
- 用户于 2026-09-12 明确回复“验收通过”，本 Spec 完成。

## 下一门禁

等待用户明确授权 `SPEC-M3-04`；未经授权不实施 Sheets 工作。
