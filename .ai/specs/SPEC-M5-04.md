# SPEC-M5-04：项目结构化台账

- 父项类型：产品需求
- 父项 ID：`M5`
- 状态：已完成
- 用户授权：已授权（2026-09-13，“验收通过，授权 SPEC-M5-04”）
- 执行顺序：4 / 5
- 更新时间：2026-09-13

## 目标

实现行动项、风险、决策和指标的 Bitable 数据模型与写入预览门禁。

## 范围

- FR-P03 至 FR-P06。
- 业务主键、字段契约、记录去重、分资源预览、依赖和部分失败报告。

## 非目标

- 不依赖 Sheets 任意区域读写，不实现删除或高风险权限操作。
- 未经真实验收，不宣称自动创建 Bitable 应用、数据表或字段可用。

## 依赖与验收

- 依赖 `SPEC-M5-03` 完成、获用户验收并明确授权本 Spec。
- 本地模型和门禁测试通过，并在专用测试资源完成前向测试和用户人工验收。

## 当前验证结果

- `SPEC-M5-03` 已通过用户人工验收，本 Spec 已获明确授权。
- 已确定复用专用测试知识库中的既有 Bitable 应用；新建 Bitable 应用不纳入本 Spec 的已验证路径。
- 已新增四类项目台账字段契约和表结构预览工作流；每张表独立预览、确认、写前回读和写后验证。
- `make-preview` 已支持项目台账建表计划，校验完整数据表分页、同名冲突、角色字段契约、业务主键、来源字段和回读计划。
- 58 项单元测试、Skill 校验和 `git diff --check` 已通过。
- 只读刷新确认测试 Bitable 当前只有 `Table` 一张数据表；已生成 `M5 行动项` 本地效果确认单和预览 `fs-e92a5da44f4ae4f9`。
- 用户确认后，预览 `fs-e92a5da44f4ae4f9` 通过写前完整表清单、状态指纹和工具门禁；已创建空表 `M5 行动项`。
- 回读确认应用现有 `Table` 保持不变，新表包含 6 个预期字段；`行动项编号` 为主字段，类型、顺序及状态选项均与确认单一致。
- 已生成 `M5 风险` 独立效果确认单和预览 `fs-51b7dd61476be614`，尚未执行该写入。
- 用户验收行动项台账并确认风险预览后，`fs-51b7dd61476be614` 通过写前门禁；已创建空表 `M5 风险`。
- 回读确认应用原有数据表保持不变，新表包含 8 个预期字段；风险编号为主字段，影响、概率和状态选项均与确认单一致。
- 已生成 `M5 决策` 独立效果确认单和预览 `fs-d6ba5f6da110634b`，尚未执行该写入。
- 用户验收风险台账并确认决策预览后，`fs-d6ba5f6da110634b` 通过写前门禁；已创建空表 `M5 决策`。
- 回读确认应用原有数据表保持不变，新表包含 6 个预期字段；`决策编号` 为主字段，字段类型和顺序均与确认单一致。
- 已生成 `M5 指标` 独立效果确认单和预览 `fs-86efb9414867f026`，尚未执行该写入。
- 用户验收决策台账并确认指标预览后，`fs-86efb9414867f026` 通过写前门禁；已创建空表 `M5 指标`。
- 回读确认应用原有数据表保持不变，新表包含 6 个预期字段；`指标编号` 为主字段，`当前值` 为数字类型，字段顺序与确认单一致。
- 四类项目台账均已创建并自动回读通过；用户于 2026-09-13 检查指标表后明确回复“验收通过，授权 SPEC-M5-05”，本 Spec 已完成。
