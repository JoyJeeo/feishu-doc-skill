# SPEC-M1B-03：局部写入与回读验证

- 父项类型：产品需求
- 父项 ID：`M1-B`
- 状态：已完成
- 用户授权：已确认预览 `fs-e25f22ab13739ae6`（2026-09-10）
- 执行顺序：3 / 3
- 更新时间：2026-09-10

## 目标

执行用户已确认的单块文本更新，并通过独立回读验证写入结果、目标范围和用户身份链路。

## 范围

- 使用 `useUAT: true` 重新读取文档版本和目标正文块。
- 校验预览编号、内容完整性、写前指纹和所需 MCP 工具。
- 仅将预览声明的空正文块更新为“M1-B 集成测试”。
- 回读文档信息、目标块、完整块列表和纯文本内容。

## 非目标

- 不修改标题或其他块。
- 不创建、删除、移动、共享或修改权限。
- 不使用应用身份或任何非飞书 MCP 通道。
- 不恢复测试文本；恢复属于新的远端写入，需要独立预览。

## 验证结果

- 写前文档版本和目标块内容与预览指纹一致。
- `scripts/feishu_guard.py` 校验预览有效。
- 局部写入成功，返回文档版本 `4`。
- 回读目标块和纯文本均为“M1-B 集成测试”。
- 回读标题仍为“TEST”，完整块列表仍只有原标题块和原正文块。
- 所有飞书调用均使用原生飞书 MCP 和 `useUAT: true`。

## 验收结论

M1-B 的“读取 → 预览 → 确认 → 写前冲突检查 → 局部写入 → 回读验证”真实集成链路通过。本 Spec 和父项 `M1-B` 已完成。

## 下一门禁

开始 M2 前，需要用户确认 M1 完成结果并明确授权 M2“飞书文档”产品需求。
