# SPEC-M2-04：真实文档集成验收

- 父项类型：产品需求
- 父项 ID：`M2`
- 状态：已完成（自动验证与用户最终人工验收均已通过）
- 用户授权：已授权只读阶段及剩余完整正文、章节编辑与幂等复核（2026-09-12）
- 执行顺序：4 / 4
- 更新时间：2026-09-12

## 目标

使用专用飞书测试资源验证 M2 的文档创建、结构回读、章节级编辑、幂等保护、格式风险报告和只读质量审计，并完成父项验收。

## 范围

- 通过用户身份读取专用测试资源并生成具体预览。
- 在逐次确认预览后验证完整文档创建和指定章节局部修改。
- 回读结构、目标范围和非目标范围，验证重复执行保护。
- 运行只读质量审计并确认没有写调用。

## 非目标

- 不读取或修改未指定资源。
- 不使用应用身份或非飞书 MCP 通道。
- 不自动清理测试资源；删除仍保持关闭。

## 依赖

- `SPEC-M2-01` 至 `SPEC-M2-03` 已完成并经用户确认。
- 用户提供专用测试文档或目录，并授权只读检查。
- 每次真实写入均有独立预览和对应 `preview_id` 确认。

## 交付物

- 真实 MCP 调用的受控验证证据。
- M2 父项验收结果、路线图和状态更新。

## 验证与验收

- 创建后的标题、块层级和内容回读一致。
- 章节编辑只改变预览声明范围，非目标章节不变。
- 重复执行同一预览不会产生新副作用。
- 格式不能保真时预览和结果均准确报告。
- 审计模式零写入；M2 退出条件全部有证据后才标记完成。
- 全部真实效果已向用户展示并获得人工验收。
- 已确认创建和回读所需的 7 个飞书 MCP 工具可见，并以用户身份读取指定测试知识库父节点、空子节点列表及首页版本 2 的全部 12 个块。
- 首页只读审计识别出默认占位内容和两个尾部空文本块；全部块均可读，未发现未知块，未调用写入工具。
- 已生成 35 块技术方案创建预览 `fs-2b51f1ee7f986cb3`，目标为在测试知识库首页下创建“M2 文档能力集成验收”；预览阶段未调用写入工具。
- 创建预览 `fs-2b51f1ee7f986cb3` 经用户确认后通过写前校验，成功创建目标子文档；首个正文写入因块参数 `invalid param` 失败。
- 写后回读确认子文档标题和父级正确、版本为 1，正文只有页面根块；旧预览已部分执行，不得重放。
- 使用只读 Markdown 转换能力验证同一内容可生成 35 个有序一级块和两张嵌套表格，并生成正文修正预览 `fs-724a86454cffadfb`。
- 修正预览 `fs-724a86454cffadfb` 经用户确认后通过写前指纹与完整性校验；单次提交 35 个一级块、共 71 个转换块时仍返回 `invalid param`。
- 写后回读确认文档仍为版本 1 且仅有页面根块；该预览已消耗，本地重复执行校验会在写入前拒绝。
- 失败原因检查确认：首次手工创建的表格块缺少单元格后代，且 `merge_info` 为空，载荷无效；第二次调用的目标与版本一致、未返回权限错误，转换块图的 ID 和父子引用自洽，失败集中在当前 MCP 的嵌套块请求参数契约。
- 两批写入预览 `fs-758414bf2ee3c94c` 仅减少单次块数量，未改变或验证嵌套块参数形状，现已撤回，禁止执行。
- 回读确认子文档仍为版本 1 且只有页面根块；高层 `docx_builtin_import` 只能新建文档，不能修复这个已存在的 Wiki 子文档。
- 已生成最小普通文本块预览 `fs-9f28e3944942c766`，拟通过 `documentBlockChildren_create` 在根块下新增一行“M2 正文写入链路验证”；预览阶段未调用写入工具。
- 最小普通文本块预览 `fs-9f28e3944942c766` 经确认并通过写前校验；写入成功后文档版本由 1 升至 2，回读确认根块和新增文本块共 2 块，正文与预览一致。
- 基础写入成功证明目标、用户权限和普通子块参数正常；此前失败范围收敛到嵌套块接口或混合载荷。
- 已将单张 2×2 Markdown 表格转换为 1 个一级表格块和 8 个后代块，并生成独立预览 `fs-1f29e5d317aded00`；预览阶段未调用写入工具。
- 表格预览 `fs-1f29e5d317aded00` 经确认并通过写前校验，但实际提交的 `merge_info` 比预览多一项，服务端返回 `invalid param`；已停止且未重试，回读确认文档仍为版本 2、无副作用，该预览已消耗。
- 已重新转换同一张 2×2 表格并生成预览 `fs-252c67c5acf657fe`；4 项 `merge_info` 与四个单元格严格对应。
- 确认 `fs-252c67c5acf657fe` 后，写前阶段发现会话内转换载荷没有跨用户回合保留；未调用写入工具并使该预览失效。
- 再次转换同一张 2×2 表格并生成预览 `fs-ab99b19024daf79c`；完整非敏感执行载荷已写入任务本地临时文件并回读核对，包含 1 个一级表格块、8 个后代块和 4 项 `merge_info`。
- 预览 `fs-ab99b19024daf79c` 经确认、写前校验和临时载荷回读后原样执行，`documentBlockDescendant_create` 仍返回 `invalid param`；写后回读确认文档版本仍为 2 且无副作用，该预览已消耗，临时载荷已删除。
- 已停用当前失败的嵌套接口，并生成空 2×2 表格分步创建预览 `fs-a142a769ba61f1c6`；拟先通过 `documentBlockChildren_create` 创建表格并回读单元格 ID，填充单元格另行预览。
- 空表预览 `fs-a142a769ba61f1c6` 经确认并通过写前校验；创建成功后文档版本由 2 升至 3，回读确认新增一个 2×2 表格、四个单元格和四个默认空文本块，原验证文本未变化。
- 已按行列顺序将四个空文本块映射到“检查项 / 状态 / 普通文本写入 / 已通过”，并生成、持久化四次定点更新预览 `fs-308f45cd9e1827fe`。
- 单元格填充预览 `fs-308f45cd9e1827fe` 经确认并通过写前校验；四次定点更新全部成功，文档版本由 3 依次升至 7。回读确认 11 个块、2×2 表格内容与行列顺序正确，原验证文本未变化，临时载荷已删除。
- 用户于 2026-09-12 检查测试子文档后明确回复“验收通过”；普通正文和分步表格写入能力正式通过人工验收。
- 用户于 2026-09-12 明确授权继续剩余完整正文、章节编辑与幂等复核；每次真实写入仍须确认独立预览。
- 回读确认目标文档当前为版本 7、共 11 个块；完整技术方案经只读转换得到 41 个扁平一级块，包括 8 个一级标题、5 个二级标题、27 个项目符号和 1 个引用。
- 已生成完整正文追加预览 `fs-ddbcd33e95d640f8`，精确非敏感执行载荷已持久化；预览阶段未调用远端写入工具。
- 完整正文预览 `fs-ddbcd33e95d640f8` 经确认并通过写前校验；写入后文档版本由 7 升至 8、总块数由 11 增至 52，41 个新增块的顺序、内容、类型和父级均与预览一致，原普通文本与表格块保持不变。
- 对 `fs-ddbcd33e95d640f8` 执行本地重复校验时，门禁以“该预览已经执行，禁止重复写入”在远端调用前拒绝重放。
- 在版本 8、共 52 个块的文档中唯一定位“验证与验收”一级章节，确定前后边界为“兼容性、迁移与回滚”和“风险”；已生成在章节末尾追加 1 个验收记录项目符号的预览 `fs-5353d84cae8f848b`，精确非敏感执行载荷已持久化，预览阶段零写入。
- 章节定点编辑预览 `fs-5353d84cae8f848b` 经确认和写前校验后执行成功；文档版本由 8 升至 9、总块数由 52 增至 53，新项目符号准确位于“验证与验收”章节末尾和“风险”之前。
- 写后对比确认 47 个章节外既有块及全部原块的内容、类型、父级和顺序保持不变；该预览的本地重复校验同样在远端调用前被拒绝。
- 最终只读审计仅调用文档信息、全量块和纯文本读取工具，零写入；版本 9 共 53 个块，8 个一级标题、5 个二级标题，无重复标题、跳级、空章节、未知块或字面量反斜杠换行。
- 只读审计记录一项低风险结构提示：为保留集成证据，最初的验证文本和 2×2 表格位于方案引用摘要之前；该项不阻塞本次验收。
- 本地最终回归：25 项单元测试通过，Skill 结构校验通过，`git diff --check` 通过。
- 用户于 2026-09-12 明确回复“验收通过”；`SPEC-M2-04` 及 M2 父项退出条件全部满足。

## 下一门禁

本 Spec 已完成。下一里程碑 M3 需要用户另行明确授权。
