# SPEC-M4-04：妙记读取与 M4 验收

- 父项类型：产品需求
- 父项 ID：`M4`
- 状态：未开始
- 执行顺序：4 / 4
- 更新时间：2026-09-13

## 目标

记录原拟实施的妙记读取范围；该范围当前已移出路线图。

## 范围

- 当前不实施妙记读取。
- 未来只有用户明确重新纳入后，才重新评估 FR-M01、用户授权和真实验收。

## 非目标

- 不实现妙记列表、搜索、文字稿、摘要、行动项或跨资源同步。

## 依赖与验收

- 依赖 `SPEC-M4-03` 完成并获用户验收。
- 真实只读结果可由用户检查，缺失文字稿时无替代通道调用；M4 当前范围退出条件通过。
- 用户于 2026-09-13 确认 `SPEC-M4-03` 并明确要求继续，本 Spec 已获授权。
- 2026-09-13 已从用户提供的文档中定位到关联妙记；基础信息、统计和媒体三个用户身份调用均返回 `99991679`，当前授权缺少妙记只读、统计和媒体导出权限，尚未取得真实读取结果。
- 用户于 2026-09-13 明确决定暂不增加妙记功能；本 Spec 已按 D-021 移出当前 M4 执行映射，不作为 M4 退出条件，先前授权不再继续执行。
