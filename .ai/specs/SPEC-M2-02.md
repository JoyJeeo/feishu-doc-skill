# SPEC-M2-02：内容生成、模板与创建预览

- 父项类型：产品需求
- 父项 ID：`M2`
- 状态：已完成
- 用户授权：已授权（2026-09-12）
- 执行顺序：2 / 4
- 更新时间：2026-09-12

## 目标

实现可复用的内容生成规则、六类个人文档模板和新建文档预览，使创建结果在写入前可完整检查。

## 范围

- 实现 FR-T01 至 FR-T03、FR-T05 至 FR-T08 和 FR-D01 的本地规划能力。
- 提供 PRD、会议纪要、项目主页、周报、技术方案和复盘模板。
- 定义目标、受众、材料、事实、推论、建议和待确认项的生成规则。
- 新建预览展示最终父级位置、标题、完整初始结构、已知格式损失、所需工具和验证方法。

## 非目标

- 不执行真实创建。
- 不实现现有文档的章节级编辑。
- 不实现图片、附件、评论或版本管理。

## 依赖

- `SPEC-M2-01` 已完成并经用户确认。
- 默认位置未配置时，用户必须在具体创建请求中提供目标位置。

## 交付物

- `references/docx/content-generation.md`
- `references/docx/templates.md`
- `workflows/docx-create-preview.md`
- `scripts/feishu_guard.py` 的新版文档创建预览结构校验
- `tests/test_feishu_guard.py` 的创建预览行为测试

## 验证与验收

- 六类模板均能形成完整、可编辑的块级结构。
- 缺少负责人、日期、指标或来源时保留待确认标记，不虚构事实。
- 创建预览包含最终位置和完整初始结构，且零远端写入。
- 全量单元测试、Skill 校验和 `git diff --check` 通过。
- 创建预览所需的 6 个飞书 MCP 工具均可见。
- 已读取用户指定测试知识库的父节点和子节点列表，生成技术方案模板创建预览 `fs-0a6b89e0d76b3550`，包含 20 个内容块；未调用写入工具。
- 真实效果已向用户展示，并于 2026-09-12 获得人工验收。

## 下一门禁

用户已于 2026-09-12 人工验收，并明确授权 `SPEC-M2-03`。
