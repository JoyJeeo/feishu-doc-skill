# SPEC-M5-05：跨资源集成验收

- 父项类型：产品需求
- 父项 ID：`M5`
- 状态：已完成
- 用户授权：已授权（2026-09-13，“验收通过，授权 SPEC-M5-05”）
- 执行顺序：5 / 5
- 更新时间：2026-09-13

## 目标

使用专用测试资源验证 M5 的跨资源依赖、链接、部分失败和回读闭环，并完成父项验收。

## 范围

- FR-T09、FR-P01 至 FR-P08 的当前 MCP 可实现子集。
- 项目主页、文档、结构化台账和周报之间的链接与状态汇总。

## 非目标

- 不补齐前序 Spec 明确排除的 MCP 缺失能力。
- 不删除测试资源，不执行所有权转移或公开权限修改。

## 依赖与验收

- 依赖 `SPEC-M5-04` 完成、获用户验收并明确授权本 Spec。
- 每个远端写入使用独立效果预览和确认；回读验证全部资源、非目标保护与失败边界，并由用户完成最终人工验收。

## 当前验证结果

- `SPEC-M5-04` 已通过用户人工验收，本 Spec 已获明确授权。
- 已新增跨资源集成结果模型、验证工作流和 `validate-project-integration` 门禁；完整结果、部分失败、依赖未验证和错误链接均有测试覆盖。
- 61 项单元测试、Skill 校验和 `git diff --check` 已通过；部分失败样例稳定返回 1 个成功、1 个失败、1 个未执行和整体 `partial`。
- 只读基线确认项目主页版本 3、共 33 个块，需求说明和周报可读；Bitable 现有 5 张表，原 `Table` 有 11 条记录，四类 M5 台账均为空且字段、记录分页完整。
- 项目主页跨资源更新预览 `fs-9f676566545d5a80` 经用户确认后通过写前版本、完整块顺序、六处原文和本地门禁校验；6 个定点更新全部成功，文档版本由 3 升至 9。
- 写后完整回读确认主页仍为 33 个块，全部块 ID、类型和顺序不变；六处新文本、四张台账直达链接及其余 27 个块均符合预览。
- 最终集成结果完整回读项目主页、需求说明、周报和四类台账；7 个资源全部 verified，9 条精确跨资源链接匹配，`validate-project-integration` 推导整体状态为 `complete`。
- 用户于 2026-09-13 检查真实主页结果后明确回复“验收通过”；本 Spec 与 M5 退出条件全部满足。
